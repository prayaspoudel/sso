# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=sso_db
DB_SSLMODE=disable

# JWT Configuration
JWT_ACCESS_SECRET=change-this-to-a-secure-random-string-for-access-tokens
JWT_REFRESH_SECRET=change-this-to-a-secure-random-string-for-refresh-tokens
JWT_ISSUER=union-products-sso

# Server Configuration
SERVER_PORT=8080
ENV=development

# CORS - Add your frontend URLs
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3004,http://localhost:3005

# Email Configuration (Optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=noreply@yourcompany.com

# Application URLs
APP_URL=http://localhost:8080
FRONTEND_URL=http://localhost:3000
